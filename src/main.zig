const std = @import("std");
const json = std.json;
const heap = std.heap;
const mem = std.mem;

const client = @import("client.zig");
const relay = @import("relay.zig");

pub fn main() !void {
    std.debug.print("noztr\n", .{});
}
